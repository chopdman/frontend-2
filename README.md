Add product flow: 
User fills out the product form
On submit, the product data is pushed into an array stored in localStorage.
The UI updates by reading the stored product list from localStorage.


React fragments : <> </>
it is used to avoid unnecessary wrapper elements.this keeps the DOM clean and improves structure without adding extra HTML tags.
